[project]
name = "clif"
version = "1.0.0"
description = "CLIF CLI File storage system (CLI Google Drive Essentially)"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.5",
]

[project.optional-dependencies]
server = [
    "flask>=3.1.3",
]
client = []