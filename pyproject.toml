[project]
name = "clif"
version = "1.0.0"
description = "CLIF CLI File storage system (CLI Google Drive Essentially)"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.3",
    "requests>=2.32.5",
]

[project]